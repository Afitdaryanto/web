---
layout: '~/layouts/MarkdownLayout.astro'
title: Snapshot
network: Mainnet
icon: bitcanna
chain: bitcanna-1
version: v1.6.3
---
## Intruction

### Install lz4
```
sudo apt install lz4 -y
```

### Stop Planqd and Backup priv_validator_state.json
```
sudo systemctl stop bcnad
cp $HOME/.bcna/data/priv_validator_state.json $HOME/.bcna/priv_validator_state.json.backup
rm -rf $HOME/.bcna/data
```

### Download Snapshot
```
curl -L https://snapshots.indonode.net/bitcanna/bitcanna-snapshot.tar.lz4 | tar -Ilz4 -xf - -C $HOME/.bcna
mv $HOME/.bcna/priv_validator_state.json.backup $HOME/.bcna/data/priv_validator_state.json
```

### Restart Node and Check the logs

```
sudo systemctl start bcnad && sudo journalctl -u bcnad -f --no-hostname -o cat
```