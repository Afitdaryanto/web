---
layout: '~/layouts/MarkdownLayout.astro'
title: Peers
icon: c4e
network: Mainnet
chain: perun-1
version: v1.1.0
---

Live Peers 

```
PEERS="$(curl -sS https://rpc.c4e.indonode.net/net_info | jq -r '.result.peers[] | "\(.node_info.id)@\(.remote_ip):\(.node_info.listen_addr)"' | awk -F ':' '{print $1":"$(NF)}' | sed -z 's|\n|,|g;s|.$||')"
sed -i -e "s|^persistent_peers *=.*|persistent_peers = \"$PEERS\"|" $HOME/.c4e-chain/config/config.toml
```