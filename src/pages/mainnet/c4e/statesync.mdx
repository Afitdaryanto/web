---
layout: '~/layouts/MarkdownLayout.astro'
title: Statesync
icon: c4e
network: Mainnet
chain: perun-1
version: v1.1.0
---

### Setup
This is the settings we use , you dont have to change anything

- snapshot-interval= 1000
- snapshot-keep-recent = 5

### Intruction

```
systemctl stop c4ed
c4ed tendermint unsafe-reset-all --home $HOME/.c4e-chain --keep-addr-book

STATE_SYNC_RPC="https://rpc.c4e.indonode.net:443"

LATEST_HEIGHT=$(curl -s $STATE_SYNC_RPC/block | jq -r .result.block.header.height)
SYNC_BLOCK_HEIGHT=$(($LATEST_HEIGHT - 1000))
SYNC_BLOCK_HASH=$(curl -s "$STATE_SYNC_RPC/block?height=$SYNC_BLOCK_HEIGHT" | jq -r .result.block_id.hash)

PEERS=094cc97f8443ba4302e2db818e64497d03176fad@rpc.c4e-chain.indonode.net:24656 
sed -i.bak -e "s/^persistent_peers *=.*/persistent_peers = \"$PEERS\"/" $HOME/.c4e-chain/config/config.toml

sed -i.bak -e "s|^enable *=.*|enable = true|" $HOME/.c4e-chain/config/config.toml
sed -i.bak -e "s|^rpc_servers *=.*|rpc_servers = \"$STATE_SYNC_RPC,$STATE_SYNC_RPC\"|" \
  $HOME/.c4e-chain/config/config.toml
sed -i.bak -e "s|^trust_height *=.*|trust_height = $SYNC_BLOCK_HEIGHT|" \
  $HOME/.c4e-chain/config/config.toml
sed -i.bak -e "s|^trust_hash *=.*|trust_hash = \"$SYNC_BLOCK_HASH\"|" \
  $HOME/.c4e-chain/config/config.toml

systemctl restart c4ed && journalctl -fu c4ed -o cat
```