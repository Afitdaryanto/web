---
layout: '~/layouts/MarkdownLayout.astro'
title: Snapshot
network: Mainnet
icon: gitopia
chain: gitopia
version: v2.0.0
---
## Intruction

### Install lz4
```
sudo apt install lz4 -y
```

### Stop Planqd and Backup priv_validator_state.json
```
sudo systemctl stop gitopiad
cp $HOME/.gitopia/data/priv_validator_state.json $HOME/.gitopia/priv_validator_state.json.backup
rm -rf $HOME/.gitopia/data
```

### Download Snapshot
```
curl -L https://snapshots.indonode.net/gitopia/gitopia-snapshot.tar.lz4 | tar -Ilz4 -xf - -C $HOME/.gitopia
mv $HOME/.gitopia/priv_validator_state.json.backup $HOME/.gitopia/data/priv_validator_state.json
```

### Restart Node and Check the logs

```
sudo systemctl start gitopiad && sudo journalctl -u gitopiad -f --no-hostname -o cat
```