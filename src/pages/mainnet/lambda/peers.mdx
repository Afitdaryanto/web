---
layout: '~/layouts/MarkdownLayout.astro'
title: Peers
icon: lambda
network: Mainnet
chain: lambda_92000-1
version: 1.0.0
---

Live Peers 

```
PEERS="$(curl -sS https://rpc.lambda.nodestake.top/net_info | jq -r '.result.peers[] | "\(.node_info.id)@\(.remote_ip):\(.node_info.listen_addr)"' | awk -F ':' '{print $1":"$(NF)}' | sed -z 's|\n|,|g;s|.$||')"
sed -i -e "s|^persistent_peers *=.*|persistent_peers = \"$PEERS\"|" $HOME/.lambdavm/config/config.toml
```