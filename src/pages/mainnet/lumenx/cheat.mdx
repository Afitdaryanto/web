---
layout: '~/layouts/MarkdownLayout.astro'
title: Node CLI Cheatsheet
icon: lumenx
network: Mainnet
chain: LumenX
version: v1.3.3
---

### Planq CLI Cheatsheet

- Always be careful with the capitalized words
- Specify `--chain-id`

### Wallet Management

Add Wallet
Specify the value `<wallet>` with your own wallet name

```
lumenxd keys add <wallet>
```

Recover Wallet
```
lumenxd keys add <wallet> --recover
```

List Wallet
```
lumenxd keys list
```

Delete Wallet
```
lumenxd keys delete <wallet>
```

Check Wallet Balance
```
lumenxd q bank balances $(lumenxd keys show wallet -a)
```

### Validator Management

Please adjust `<wallet>` , `MONIKER` , `YOUR_KEYBASE_ID` , `YOUR_DETAILS` , `YOUUR_WEBSITE_URL`

Create Validator (Staking) with 10K Acre
```
lumenxd tx staking create-validator -y \
  --chain-id LumenX \
  --moniker <YOUR_MONIKER> \
  --pubkey "$(lumenxd tendermint show-validator)" \
  --amount 10000000000000000000000ulumen \
  --identity "YOUR_KEYBASE_ID" \
  --website "YOUUR_WEBSITE_URL" \
  --details "YOUR_DETAILS" \
  --from wallet \
  --commission-rate=0.05 \
  --commission-max-rate=0.20 \
  --commission-max-change-rate=0.01 \
  --min-self-delegation 1 \
  --gas auto \
  --gas-adjustment="1.15" \
  --gas-prices="0.0025ulumen"
```

Edit Validator
```
lumenxd tx staking edit-validator \
--moniker="<MONIKER>" \
--identity="YOUR_KEYBASE_ID" \
--details="YOUR_DETAILS" \
--website="YOUR_WEBSITE_URL"
--chain-id LumenX \
--commission-rate=0.05 \
--from=<wallet> \
--gas-adjustment="1.4" \
--gas auto \
--gas-prices="0.0025ulumen" \
-y
```


Unjail Validator
```
lumenxd tx slashing unjail --from <wallet> --chain-id --chain-id LumenX --gas-adjustment="1.15" --gas auto --gas-prices="0.0025ulumen" -y
```

Check Jailed Reason
```
lumenxd query slashing signing-info $(lumenxd tendermint show-validator)
```

### Token Management

Withdraw Rewards
```
lumenxd tx distribution withdraw-all-rewards --from wallet --chain-id LumenX --gas-adjustment="1.15" --gas auto --gas-prices="0.0025ulumen" -y
```

Withdraw Rewards with Comission
```
lumenxd tx distribution withdraw-rewards $(lumenxd keys show wallet --bech val -a) --commission --from wallet --chain-id LumenX --gas-adjustment="1.15" --gas auto --gas-prices="0.0025ulumen" -y
```

Delegate Token to your own validator
```
lumenxd tx staking delegate $(lumenxd keys show wallet --bech val -a) 1000000ulumen --from wallet --chain-id LumenX --gas-adjustment="1.15" --gas auto --gas-prices="0.0025ulumen" -y
```

Delegate Token to other validator
```
lumenxd tx staking redelegate $(lumenxd keys show wallet --bech val -a) <TO_VALOPER_ADDRESS> 1000000ulumen --from wallet --chain-id LumenX --gas-adjustment="1.15" --gas auto --gas-prices="0.0025ulumen" -y
```

Unbond Token from your validator
```
lumenxd tx staking unbond $(lumenxd keys show wallet --bech val -a) 1000000ulumen --from wallet --chain-id LumenX --gas-adjustment="1.15" --gas auto --gas-prices="0.0025ulumen" -y
```

Send Token to another wallet
```
lumenxd tx bank send wallet <TO_WALLET_ADDRESS> 1000000ulumen --from wallet --chain-id LumenX
```

### Governance 

Vote
You can change the value of `yes` to `no`,`abstain`,`nowithveto`

```
lumenxd tx gov vote 1 yes --from wallet --chain-id LumenX --gas-adjustment="1.15" --gas auto --gas-prices="0.0025ulumen" -y
```

### Other

Set Your own Custom Ports
You can change value `CUSTOM_PORT=24` To any other ports
```
CUSTOM_PORT=24
sed -i.bak -e "s%^proxy_app = \"tcp://127.0.0.1:26658\"%proxy_app = \"tcp://127.0.0.1:${CUSTOM_PORT}658\"%; s%^laddr = \"tcp://127.0.0.1:26657\"%laddr = \"tcp://127.0.0.1:${CUSTOM_PORT}657\"%; s%^pprof_laddr = \"localhost:6060\"%pprof_laddr = \"localhost:${CUSTOM_PORT}060\"%; s%^laddr = \"tcp://0.0.0.0:26656\"%laddr = \"tcp://0.0.0.0:${CUSTOM_PORT}656\"%; s%^prometheus_listen_addr = \":26660\"%prometheus_listen_addr = \":${CUSTOM_PORT}660\"%" $HOME/.lumenx/config/config.toml
sed -i.bak -e "s%^address = \"tcp://0.0.0.0:1317\"%address = \"tcp://0.0.0.0:${CUSTOM_PORT}317\"%; s%^address = \":8080\"%address = \":${CUSTOM_PORT}080\"%; s%^address = \"0.0.0.0:9090\"%address = \"0.0.0.0:${CUSTOM_PORT}090\"%; s%^address = \"0.0.0.0:9091\"%address = \"0.0.0.0:${CUSTOM_PORT}091\"%; s%^address = \"0.0.0.0:8545\"%address = \"0.0.0.0:${CUSTOM_PORT}545\"%; s%^ws-address = \"0.0.0.0:8546\"%ws-address = \"0.0.0.0:${CUSTOM_PORT}546\"%" $HOME/.lumenx/config/app.toml
```

Enable Indexing usually enabled by default
```
sed -i -e 's|^indexer *=.*|indexer = "kv"|' $HOME/.lumenx/config/config.toml
```

Disable Indexing
```
sed -i -e 's|^indexer *=.*|indexer = "null"|' $HOME/.lumenx/config/config.toml
```

Reset Chain Data
```
lumenxd tendermint unsafe-reset-all --home $HOME/.lumenx --keep-addr-book
```

### Delete Node

WARNING! Use this command wisely 
Backup your key first it will remove Planq

```
sudo systemctl stop lumenxd && \
sudo systemctl disable lumenxd && \
rm /etc/systemd/system/lumenxd.service && \
sudo systemctl daemon-reload && \
cd $HOME && \
rm -rf .lumenx && \
rm -rf $(which lumenxd)
```
