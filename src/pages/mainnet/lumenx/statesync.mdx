---
layout: '~/layouts/MarkdownLayout.astro'
title: Statesync
icon: lumenx
network: Mainnet
chain: LumenX
version: v1.4.0
---

### Setup
This is the settings we use , you dont have to change anything

- snapshot-interval= 1000
- snapshot-keep-recent = 5

### Intruction

```
systemctl stop lumenxd
lumenxd tendermint unsafe-reset-all --home $HOME/.lumenx --keep-addr-book

STATE_SYNC_RPC="https://rpc.lumenx.indonode.net:443"

LATEST_HEIGHT=$(curl -s $STATE_SYNC_RPC/block | jq -r .result.block.header.height)
SYNC_BLOCK_HEIGHT=$(($LATEST_HEIGHT - 1000))
SYNC_BLOCK_HASH=$(curl -s "$STATE_SYNC_RPC/block?height=$SYNC_BLOCK_HEIGHT" | jq -r .result.block_id.hash)

PEERS=8246854d88bbba7acec7b4d86c9b418c90816f1f@rpc.lumenx.indonode.net:24656 
sed -i.bak -e "s/^persistent_peers *=.*/persistent_peers = \"$PEERS\"/" $HOME/.lumenx/config/config.toml

sed -i.bak -e "s|^enable *=.*|enable = true|" $HOME/.lumenx/config/config.toml
sed -i.bak -e "s|^rpc_servers *=.*|rpc_servers = \"$STATE_SYNC_RPC,$STATE_SYNC_RPC\"|" \
  $HOME/.lumenx/config/config.toml
sed -i.bak -e "s|^trust_height *=.*|trust_height = $SYNC_BLOCK_HEIGHT|" \
  $HOME/.lumenx/config/config.toml
sed -i.bak -e "s|^trust_hash *=.*|trust_hash = \"$SYNC_BLOCK_HASH\"|" \
  $HOME/.lumenx/config/config.toml

systemctl restart lumenxd && journalctl -fu lumenxd -o cat
```