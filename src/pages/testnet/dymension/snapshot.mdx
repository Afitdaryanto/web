---
layout: '~/layouts/MarkdownLayout.astro'
title: Snapshot
network: Testnet
icon: dymension
chain: 35-C
version: v0.2.2-beta
---
## Intruction
For this snapshot I use kjnodes snapshot , it said updated every 6 hours

### Install lz4
```
sudo apt install lz4 -y
```

### Stop Planqd and Backup priv_validator_state.json
```
sudo systemctl stop dymd
cp $HOME/.dymension/data/priv_validator_state.json $HOME/.dymension/priv_validator_state.json.backup
rm -rf $HOME/.dymension/data
```

### Download Snapshot
```
SOON
```

### Restart Node and Check the logs

```
sudo systemctl restart dymd && sudo journalctl -u dymd -f --no-hostname -o cat
```